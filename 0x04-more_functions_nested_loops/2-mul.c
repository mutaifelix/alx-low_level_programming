#include "main.h"
#include <stdio.h>
/**
 * mul - function that multiply two numbers
 * @a: first number
 * @b: second number
 * Return: returns result
 */

int mul(int a, int b)
{
	return (a * b);
}
